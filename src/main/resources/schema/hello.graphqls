type Query {
    # Public queries removed - GraphQL requires authentication
    # All queries are now defined in user.graphqls with proper authorization
}

type Mutation {
    # User mutations will be extended in user.graphqls
}

type Greeting {
    message: String!
}